import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  outDir: '../docs',
  // TODO: set these after forking
  // site: 'https://YOUR_USERNAME.github.io',
  // base: '/YOUR_REPO_NAME',
});
